require 'spec_helper'

describe Devise::BootstrapForm do
  it 'has a version number' do
    expect(Devise::BootstrapForm::VERSION).not_to be nil
  end
  #
  # it 'does something useful' do
  #   expect(false).to eq(true)
  # end
end
